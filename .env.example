# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Anthropic (script generation)
ANTHROPIC_API_KEY=

# Instagram / Meta Graph API
META_APP_ID=
META_IG_APP_ID=
META_APP_SECRET=
META_REDIRECT_URI=

# Reel transcription (all optional — the feature degrades gracefully if unset).
# Permalink-based providers (primary working path for inspiration reels), tried
# in this order:
WAYINVIDEO_API_KEY=
GETTRANSCRIBE_API_KEY=
# Generic configurable fallback (e.g. Subclip or a RapidAPI-hosted endpoint):
REEL_TRANSCRIPT_API_URL=
REEL_TRANSCRIPT_API_KEY=
REEL_TRANSCRIPT_API_HOST=
# Audio-based Whisper providers (used only when a downloadable reel video exists):
GROQ_API_KEY=
HF_API_TOKEN=
