export function TopBar() {
  return (
    <header className="sticky top-0 z-10 border-b border-[#1f1f1f] bg-[#0d0d0d]/95 px-8 py-4 backdrop-blur">
      <div className="text-sm font-mono text-zinc-300">ReelSpy Command Center</div>
    </header>
  );
}
