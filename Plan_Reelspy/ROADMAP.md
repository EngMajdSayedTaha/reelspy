# ReelSpy — Roadmap & Status

Update this file at the end of every session. Status values: `todo` · `in-progress` · `blocked` · `done`.

## LAUNCH (blockers only — ~23 evenings ≈ 7–8 weeks at 10–15 h/wk)

Execution order: L4/L8 → L1 → L2 → L3 → L5 → L6 → L7 → L10/L11 → L9 → L12 → L13.
(L6 before L7 so onboarding can show tiers.)

| # | Item | Tag | Effort | Status | Notes |
|---|---|---|---|---|---|
| L4 | Rate-limit the unmetered heavy routes (B6) | BLOCKER | 0.5 ev | done | throttled reel-from-link (shares `transcript` bucket) + new `upload_presign` limit; diag `?transcribe=1` gated behind `DIAG_ALLOWED_USER_IDS` (fail-closed) |
| L8 | Cron cadence / Vercel Pro (B5) | BLOCKER | 0.5 ev | done | `publish-due */5` + `poll-comments */10` **temporarily removed from `vercel.json`** (Hobby caps at 2 daily crons) — only the 2 daily crons ship until Vercel Pro; one-step restore documented in `docs/cron-cadence.md`. "Post now" unaffected (inline dispatch); automations still fire via webhook `after()` |
| L1 | De-persona AI prompts (B2) | BLOCKER, WEDGE | 2 ev | done | `brand_voice` jsonb on profiles; system prompts now built from it with neutral fallback; both AI routes pass it. Collection UI lands in L7 |
| L2 | Transcript-grounded scripts (W1) | WEDGE | 2 ev | done | route feeds `<reel_transcript>` + perf stats into the prompt; `grounded` flag → chip in ScriptGenerator; one-tap "transcribe first, then regenerate" |
| L3 | Claude for paid tiers + tool-use JSON (W2) | WEDGE | 2 ev | done | `chat()` routes paid→Claude (Haiku 4.5 / Sonnet 4.6 for Pro/Studio), free→NVIDIA; `emit_script`/`emit_growth_notes` forced tool-use (clean JSON, strict). Tier via env `AI_DEFAULT_TIER` until L6 |
| L5 | Instrumentation + WLC views (§5) | BLOCKER | 2 ev | done | `app_events`+`ai_usage` tables, `track()`/`trackAiUsage()` helper, 5 SQL views (WLC, activation+SLA, retention, publish success, AI cost). Core funnel/WLC/AI-cost events wired; publish/automation/scheduled events deferred (see log) |
| L6 | Stripe UAE billing + entitlements (B1) | BLOCKER | 7 ev | done | `subscriptions` table (Stripe-written, owner-read RLS) + monthly-quota RPC; `lib/billing/{entitlements,plans,subscription,quota,stripe}.ts`; enforcement at 4 chokepoints (accounts/scripts-mo/transcripts-mo/automations); checkout/portal/webhook routes; `/dashboard/billing` + sidebar plan badge. Keys pending Stripe UAE approval — routes 503 until set (docs/billing-setup.md) |
| L7 | Onboarding to <10-min activation (B3) | BLOCKER, WEDGE | 3 ev | done | `profiles.onboarded_at` + `lib/onboarding/state.ts` (step inferred from data); `/dashboard/onboarding` URL-driven 4-step wizard (connect/starter-pack → brand voice → accounts → first script); `seedStarterPack` (zero-Meta-quota from `ig_account_snapshots`, trimmed to plan cap); `BrandVoiceForm` (writes `brand_voice`); dashboard setup-checklist card + first-run redirect; `onboarding_step` events wired |
| L10 | Empty/error/loading states (B7) | BLOCKER | 1.5 ev | done | 6 `loading.tsx` skeletons (scripts/accounts/generate/publishing/automations/my-account); feed first-run empty state routes zero-account users to onboarding; ScriptGenerator upfront degraded-grounding hint (failed/pending/none). ScriptsList empty + SyncButton retry already covered |
| L11 | Palette rebrand + token hygiene (B9) | BLOCKER-adj | 1 ev | done | `globals.css` → violet `#6D28D9`/`#7C3AED` primary + white fg, ink/slate dark surfaces, emerald `--brand`; new `--success/--warning/--danger/--info` tokens; swept ~90 hardcoded amber/emerald/rose/sky/blue classes across 41 files; `text-black`→`text-primary-foreground`; violet sheen. Decorative color (IG gradient/heart) preserved |
| L9 | Publish failure notifications + retry (B4) | BLOCKER | 2 ev | done | dispatcher now writes honest `partial` status (some targets landed, some failed) computed over ALL jobs so retries recompute correctly; `lib/email/send.ts` (Resend HTTP, fail-open) + `notifyPublishFailure` summary email w/ per-platform errors + deep link; `partial` badge + page-level "needs attention" banner; `publish_job_finished` event wired (feeds publish_success view). Retry action already existed. Keys pending: `RESEND_API_KEY`/`EMAIL_FROM` (emails no-op until set) |
| L12 | Terms page + privacy update + delete/export (B8+H6) | BLOCKER | 4 ev | done | `/terms` (subscription/refund/AUP/UAE law) + footer/login links; `/privacy` rewritten with sub-processor list (incl. Resend), breach-notification + UAE PDPL contact, self-serve export/delete copy; `GET /api/account/export` (JSON bundle, tokens stripped, `account_export` rate-limited) + `POST /api/account/delete` (typed-DELETE confirm → Meta `/me/permissions` revoke → R2 video cleanup → explicit app_events/ai_usage + profiles delete → deleteUser). Settings "Data & privacy" card (export link + delete dialog). **No migration** — pure code/copy. Verified live FKs: profiles→auth.users is NO ACTION so profiles must be deleted before deleteUser (route does) |
| L13 | Vitest + money-logic tests (H5, entitlements slice) | BLOCKER-adj | 1.5 ev | done | Vitest 4 (`vitest.config.ts` node env + `@/` alias), `test`/`test:watch` scripts; **54 tests / 6 files** covering the money path — entitlements caps+withinLimit+unlimited, plans Stripe price wiring + `tierForStripePrice` webhook reverse-lookup (mis-assign guard), subscription active-status + invalid-tier→free + fail-open, `consumeMonthlyQuota` allow/deny/unlimited-short-circuit/fail-open, `resolveUserTier` sub-wins-vs-env-default, and `matchKeyword` (contains/exact/any, Arabic+emoji boundaries, regex-literal). Fake Supabase helper (no live DB). Added `.github/workflows/ci.yml` (typecheck + tests on push/PR). Deferred (H5 optional): JSON-repair + RPC integration test |

**Launch total:** ~23 evenings.

## V1.1 — retention + publishing GA (~13 evenings)

| # | Item | Tag | Effort | Status |
|---|---|---|---|---|
| V1 | Persistent hook library w/ tags (W4) | WEDGE, MOAT | 2 ev | done |
| V2 | Auto-transcribe top reels post-sync (W5) | WEDGE | 1.5 ev | done |
| V3 | Weekly niche digest email (W6) | WEDGE | 2.5 ev | done |
| V4 | Durable job queue + publish/transcribe migration (H1) | MOAT | 3 ev | done |
| V5 | Relative "Outperforming" score + why-tooltip (W3) | WEDGE, MOAT | 1.5 ev | done |
| V6 | Publishing GA: honest `partial` status, calendar consolidation, TikTok/YouTube app-audit follow-through | BLOCKER→V1.1 | 2 ev | done |
| V7 | Event-log retention cron; dead-code sweep | — | 0.5 ev | done |

## V2 — Arabic, Studio, GCC dataset

| # | Item | Tag | Status |
|---|---|---|---|
| X1 | Arabic UI: locale pref, `dir` plumbing, logical-property completion, Arabic font | MOAT | done |
| X2 | Arabic-first script generation presets (Gulf dialect vs MSA toggle) | WEDGE, MOAT | done |
| X3 | GCC niche dataset: cross-user trending per niche | MOAT | done |
| X4 | Studio multi-account: multiple IG connections per user | MOAT | todo |
| X5 | Research platform abstraction consumed by a TikTok source (H2) | MOAT | todo |

## Explicitly deprioritized (do not invest without a reason)
- YouTube auto-reply — keep running, freeze investment, consider hiding on Free/Creator.
- TikTok publishing adapter — dormant until Meta/TikTok app audit passes.
- Growth-notes typed-reveal UI polish — cosmetic, no further investment.
- `/dashboard/my-account` insights charts expansion — maintain, don't grow.

## Session log
<!-- Append one entry per session: date, item(s) touched, what shipped, what's left, decisions made -->
- **2026-07-04 — X3 (V2), GCC niche dataset — cross-user "Niche Radar". THE MOAT (per project-strategy).** No migration (service-role aggregation over the existing shared snapshot cache — zero DDL, matching the no-MCP constraint). **Niche taxonomy discovery:** `inspiration_accounts.niche_tags` exists in the schema but is **never read or written by any code** (dead schema) — so the real per-account category signal is `account_groups.name` (how users explicitly file accounts). X3 aggregates *normalized group names across ALL users* into an emergent niche taxonomy. **`lib/trends/niche.ts`** (server-only, admin client — it intentionally crosses user boundaries, the whole point of aggregate intel): `loadNicheIndex` joins every user's grouped `inspiration_accounts` against `account_groups` → niche→accounts / niche→taggers maps + the global tracked-account set; `listNiches` returns niches with ≥`minAccounts` distinct accounts (popularity-sorted); `nicheTrending({niche, days, limit})` pulls `ig_reel_snapshots` for the niche's accounts in-window (chunked `.in()`), computes each account's **median** viral_score (score recomputed in JS from raw counts via `viralScore`, mirroring `tracked_reels.viral_score` since the cache doesn't store it), and ranks reels by **audience-normalized relative_score = score / max(followers,1000)** with an **outperform_ratio = score / account_median** (size control — a small niche account's true outlier surfaces above a giant's baseline). **Anonymized:** output is public account + public metrics + cross-user counts only, NEVER which user tracks what. **`/dashboard/trends` (Niche Radar)** page: `NichePicker` (chips desktop / select mobile, `?niche=` param, "All niches" = whole userbase) + responsive `TrendReelCard` grid (thumbnail, outperform ×N badge, @account + followers, metric row, "View" + "Track") + honest empty state + `loading.tsx`. **`trackNicheAccount` action** turns discovery into the user's own funnel: seeds `inspiration_accounts` from the snapshot cache (**zero Meta quota**, no Business Discovery — unlike `addInspirationAccount`), cap-enforced (L6), and find-or-creates a group named for the niche so the categorization carries into their feed; fires `account_added {source:"niche_radar"}`. Wired into Sidebar nav (Radar icon, after Feed) + TopBar title + i18n dict (`nav.trends`/`titles.trends`, en "Niche Radar" / ar "رادار المجال"). **Client-bundle split** (same class as X2): `NichePicker` importing the runtime `ALL_NICHES` from the `server-only` niche module broke the Turbopack build → extracted client-safe constants/types/pure-helpers (`ALL_NICHES`, `slugifyNiche`, `viralScore`, `NicheSummary`, `TrendReel`) into **`lib/trends/shared.ts`** (no server-only); `niche.ts` imports + re-exports them; client components + the action import from `shared.ts`. New `test/trends/niche.test.ts` (6 cases incl. the **core moat proof**: a 5k-follower account's outlier ranks #1 over a 1M-follower account's baseline reel; niche group-name aggregation across users; ALL_NICHES spans ungrouped accounts) → **81 tests / 11 files** green; typecheck + `next build` (EXIT 0, `/dashboard/trends` registered) clean. Decisions: **group names as the niche signal** (niche_tags is dead; groups are populated + explicit); **JS aggregation via admin client over a SQL RPC** (no DDL this session + moderate volume; a `security definer` RPC like V5's outperforming_feed is the future optimization — noted in the file); size-control via relative_score is the anti-"big accounts drown small ones" mechanism the strategy memo demanded; track-from-cache (zero quota) keeps the discovery→track loop friction-free. Left: RPC-ify when DDL is available (perf at scale + true SQL medians/pagination); consider surfacing top *hooks* per niche (X3's "top hooks in UAE real-estate this week" idea) as a follow-up on the same substrate; niche quality depends on users actually grouping accounts (degrades gracefully to "All niches"). Next: X4 (Studio multi-account).
- **2026-07-04 — X2 (V2), Arabic-first script generation presets (Gulf vs MSA).** No migration (rides the existing `brand_voice` jsonb — zero DDL). Added a structured `arabicDialect: "gulf"|"msa"|null` preset to `BrandVoice` (separate from the free-text `language` field). When set, `buildScriptSystemPrompt` injects a hard **LANGUAGE REQUIREMENT** directive (`arabicDialectDirective`) that forces the whole script — hook/body/CTA — into Arabic in native phrasing (no transliteration), **overriding the reel's language and the Primary-language field**: Gulf → colloquial Khaleeji (خليجي) as spoken in the UAE/GCC with an explicit "do NOT use MSA" steer; MSA → الفصحى, pan-Arab, still short-form-natural. `saveBrandVoice` reads + guards the value (`isArabicDialect`, unknown→null). `BrandVoiceForm` gained an "Arabic script preset" select (Off / Gulf (خليجي) / MSA (الفصحى)). **Module split to fix a client-bundle regression:** importing the runtime `ARABIC_DIALECTS` const into the client `BrandVoiceForm` from `lib/ai/claude.ts` dragged the Anthropic SDK into the browser bundle (Turbopack build error). Extracted the dependency-free brand-voice API — `BrandVoice`, `ArabicDialect`, `ARABIC_DIALECTS`, `isArabicDialect` — into new **`lib/ai/brand-voice.ts`** (no provider/SDK imports); `claude.ts` imports + re-exports them (server importers unchanged), while the client form + onboarding action import from `brand-voice.ts` directly. New `test/ai/arabic-preset.test.ts` (6 cases: dialect guard, EN/AR label presence, directive present for gulf/msa with the right steer, absent when unset, unknown value ignored) → **75 tests / 10 files** green; typecheck + `next build` (EXIT 0) clean. Decisions: structured toggle over relying on the free-text language field (deterministic Arabic output is the wedge for GCC creators); preset overrides source-reel language (a Gulf creator wants Gulf output even from an English reel); applied to scripts only (growth notes out of scope). Built on X1. Left: brand voice is still only editable via the onboarding wizard (no dedicated Settings surface — pre-existing; re-running onboarding shows current values); a settings entry point is a small future follow-up. Next: X3 (the moat).
- **2026-07-04 — X1 (V2), Arabic UI foundation. First V2 item.** No migration (locale rides the existing device-local prefs cookie — zero DDL, matching the no-MCP-this-session constraint). **Locale plumbing:** added `locale: "en"|"ar"` to `UserPrefs` (`lib/prefs.ts`, parsed via `normalizeLocale`, default `en`). New pure/client-safe `lib/i18n/config.ts` (`LOCALES`, `DEFAULT_LOCALE`, `LOCALE_LABELS`, `isLocale`/`normalizeLocale`, `dirForLocale`). **dir/lang:** `app/layout.tsx` is now `async`, reads the prefs cookie server-side and sets `<html lang dir>` from the locale; the Toaster corner mirrors (`top-left` in RTL). **Arabic font:** loaded `IBM_Plex_Sans_Arabic` (arabic subset, weights 400–700) as `--font-arabic`; `globals.css` swaps it in under `html[lang="ar"] body` (Geist is latin-only, so Arabic glyphs fell back to system fonts before). **Translated shell (i18n scaffolding):** `lib/i18n/dictionaries.ts` — `en`/`ar` dictionaries typed by `Dict = typeof en` (English defines the required key shape → a missing translation is a compile error, not a silent English fallback), covering nav + top-bar titles + shell chrome + prefs labels. `getDictionary(locale)` is read server-side in `app/dashboard/layout.tsx` and threaded through `DashboardShell` → `Sidebar`/`TopBar` as a `dict` prop (SSR-correct, no hydration flash). Sidebar nav uses `labelKey` into `dict.nav`; TopBar titles use `dict.titles` (also filled in the previously-missing publishing/connections/billing rows). **Logical-property conversion** (high-traffic chrome per 04-ui-ux RTL debt): Sidebar drawer `left-0`→`start-0`, `border-r`→`border-e`, hidden-state transform now `-translate-x-full rtl:translate-x-full` so it tucks off the correct edge; active-indicator `left-0`→`start-0`; DashboardShell main margin `lg:ml-[240px]`→`lg:ms-[240px]`. **Language selector:** added to `PreferencesForm` (writes `locale` via `savePreferences`); on change it does a full `window.location.reload()` so the root-layout-rendered `<html dir>` re-renders (revalidatePath alone won't re-run the root layout on a client nav). New `test/i18n/locale.test.ts` (dir mapping, normalize/isLocale guards, **dictionary key-parity across locales** + non-empty leaves, prefs locale round-trip/reject) → **69 tests / 9 files** green; typecheck + `next build` (EXIT 0, Arabic font fetched) clean. Decisions: cookie over a DB column (no DDL, device-local is fine for a UI pref, instant SSR read); dictionary-typed-by-English so translations can't silently drift; foundation scope = shell only (page-body copy translates incrementally on the same `dict` mechanism — deliberately not gold-plated). This **unblocks X2** (Arabic script presets). Left: page-body strings still English (expand `dict` per page over time); the terminal-path breadcrumb (`~/reelspy`) left LTR by design. Next: X2.
- **2026-07-04 — V7, event-log retention cron + dead-code sweep. V1.1 COMPLETE.** No migration (pure code — the Supabase MCP requires interactive auth and wasn't available this session, so retention is done with service-role admin deletes, no DDL). **(A) Retention cron.** `lib/analytics/retention.ts` `pruneEventLogs(admin, now?)`: deletes rows older than `EVENT_RETENTION_DAYS` (default 365) from the three append-only telemetry tables — `app_events`, `ai_usage`, `automation_events` (the last holds third-party comment text → the real PDPL data-minimization case, extends H6's note) — plus terminal `jobs` (`done`/`failed` only; queued/running untouched) older than `JOBS_RETENTION_DAYS` (default 30) for queue-table hygiene. Each table pruned in its own try/catch so one failure doesn't block the rest; returns per-table counts + errors. `count:"exact"` gives deletion counts without pulling row payloads back. Route `app/api/cron/prune-events/route.ts` (cronAuthorized, admin client) returns 207 on partial failure so the workflow log flags it. Triggered by `.github/workflows/prune-events.yml` (Sun 04:00 UTC, off-peak away from Mon-08:00 digest) — same GH-Actions-over-Vercel-cron posture as weekly-digest/run-jobs (Hobby's 2 daily slots spent on refresh-snapshots/refresh-tokens). Env + `docs/cron-cadence.md` updated. **This app's oldest data is only months old, so at 365 days the prune is a proactive no-op until ~mid-2027** — running it weekly from now means the eventual delete set is only ever a single week's expired rows (never a giant backlog), so a plain per-table delete is safe (no batching needed). **(B) Dead-code sweep.** Removed `getReelInfo()` + its `ReelInfo` type from `lib/media/ytdlp.ts` (exported, never imported — audit item 8; its helpers `resolveBinary`/`cookieArgs`/`execFileAsync` stay, still used by `getReelMetadata`). **Left migration history untouched** — the churn pairs (`20260607_reel_patterns`→`20260616`, `20260614_auto_reply_like`→`20260625b`) are history noise only; `schema.sql` is authoritative and the 2026-07-03 rebaseline already aligned `schema_migrations`, so editing files there would risk breaking the `db push` no-op. New `test/analytics/retention.test.ts` (4 cases: all-four-tables + summed counts, 365/30-day cutoff math, jobs `in(done,failed)` filter, per-table-error-doesn't-abort) → **63 tests / 8 files** green; typecheck + `next build` clean (route `/api/cron/prune-events` registered). Decisions: code-only deletes over a SQL RPC (can't apply DDL this session + trivial volume + service role already owns these tables); weekly cadence (12-month retention doesn't need finer); 207-not-500 on partial failure so a single bad table still lets the others prune. Left: set repo `CRON_SECRET`+`APP_BASE_URL` for the new workflow (same one-time setup as the other GH crons); optionally re-home to a Vercel `*/…` cron once on Pro. **All V1.1 items (V1–V7) are now `done`.** Next: V2 (Arabic/Studio/GCC dataset — X1–X5), not started.
- **2026-07-04 — V6 (Publishing GA), calendar consolidation + TikTok/YouTube audit follow-through.** No migration (pure code). Part 1 (honest `partial` status) was already shipped at L9, so V6 reduced to two pieces. **(A) Calendar consolidation** — the calendar is now the single scheduling surface for publishing, not just a read-only mirror of scheduled posts. New `reschedulePost({postId, scheduledAt})` server action (`publishing/actions.ts`) — thin sibling of `updateScheduledPost`: only-`scheduled` guard, moves `scheduled_at`, and applies the **same durable-job sync** (bump the queued `publish:<id>` job's `run_at`, or enqueue if none). `CalendarView` now: drag payloads are **type-prefixed** (`script:<id>` vs `post:<id>`) so a day cell routes a dropped script to `scheduleScript` and a dropped post to `reschedulePost`; **scheduled posts are draggable** and reschedule **preserving local time-of-day** (calendar knows only the target day, so the hour comes from the existing slot; client composes the ISO); the unscheduled tray ignores post payloads (a post can't be "unscheduled"). Post status now renders **honestly** on the calendar (dots + desktop chips + detail pill via `POST_STATUS_DOT`/`POST_STATUS_CHIP`, in lockstep with the Publishing page's StatusBadge — scheduled=info, publishing/partial=warning, done=success, failed=danger) instead of a flat `bg-primary`; added a `+N more` for posts and a **legend** distinguishing the planning layer (scripts) from the publish layer (posts). **(B) TikTok/YouTube audit follow-through** — wired the existing `TIKTOK_ALLOW_PUBLIC`/`YOUTUBE_ALLOW_PUBLIC` env flags (already read by the adapters) up to the UI: `publishing/page.tsx` computes `publicAllowed: Record<Platform,boolean>` server-side and passes it to `PublishComposer`, which replaced the always-on generic "TikTok & YouTube stay private" line with a **per-selection honest notice** — warns (warning color) only about the pre-audit platforms **actually selected** when visibility=public, falls back to a subtle upfront note for connected-but-locked platforms, and **disappears entirely** once the founder flips the flags post-audit. `PublishPreview` reflects the **effective** visibility for the shown platform (a "public" choice on a locked platform renders "Private · until audit"). Decisions: reschedule preserves time-of-day rather than snapping to a default hour (least-surprise for a drag); posts stay on the calendar (drag = reschedule only, no unschedule-to-tray since a post must go live somewhere); env-flag-driven UI means zero code change when the audit passes — just set the flag. Left (unchanged, non-code): TikTok/YouTube app audits themselves are external (Meta/TikTok/Google dashboards); the flags stay unset until they pass. Typecheck + `next build` + 59 tests all clean. Next todo: V7 (event-log retention cron + dead-code sweep).
- **2026-07-04 — V4 (H1), durable job queue + publish/transcribe/digest migration.** Migration `20260704064241_jobs_queue.sql` (applied + verified live via MCP; schema.sql mirrored): `jobs` table (kind/payload/user_id→profiles cascade/run_at/attempts/max_attempts/locked_at/locked_by/status queued|running|done|failed/last_error/dedup_key), RLS on with **no policies** (service-role only, INFO advisor confirms intended posture), partial index `jobs_due_idx` on `(status, run_at)` for the claim scan + partial unique `jobs_dedup_active_idx on (dedup_key) where status in (queued,running)` = single-in-flight per key. **`claim_jobs(worker, kinds[], limit, lock_timeout=600)`** SECURITY DEFINER RPC: atomic `update … where id in (select … for update skip locked)` — same discipline as `consume_user_action` — claims due `queued` rows PLUS stale-locked `running` rows (crashed-worker reclaim), bumps `attempts` on claim so a mid-run crasher still converges on max_attempts; `revoke from anon/authenticated`, `grant service_role` (absent from the security-definer WARN list — not web-exposed). Smoke-tested live (enqueue→claim→running/locked; dedup blocked a duplicate). `lib/jobs/queue.ts`: typed `enqueueJob` (23505 on the dedup index → `{skipped:true}` no-op), `claimJobs`, `completeJob`, `failJob` (exponential backoff `backoffMs` base 60s→cap 1h, reschedule until max_attempts then park `failed`). Worker `app/api/cron/run-jobs/route.ts` (cronAuthorized, budget-bounded ~260s): reconciles due-but-unqueued scheduled posts → jobs, claims a batch across all kinds, runs by kind, `after()` self-refetch to drain a backlog. **Three producers migrated:** (1) **publish** — `createPublishPost` enqueues a `publish_post` job at `scheduledAt` (dedup `publish:<id>`), `updateScheduledPost` bumps the queued job's run_at; worker runs the unchanged idempotent `dispatchPost`; **retired `/api/cron/publish-due`** (folded into run-jobs). (2) **transcribe** — post-sync `after()` now `enqueueTopReelTranscriptions` (fast fan-out, dedup `transcribe:<reelId>`) instead of transcribing inline; new `lib/media/transcribe-job.ts runTranscribeReel` does the Whisper work in the worker with the **exact same hourly→monthly quota guards**, throttle→reschedule, cap→release, fail→mark reel failed. (3) **digest** — `weekly-digest` cron fans out one `send_digest` job per opted-in user (dedup `digest:<isoWeek>:<uid>`, maxAttempts 3); `lib/email/digest-job.ts runSendDigest` builds+sends one user's digest in the worker (re-checks opt-out at send time). **Trigger:** `.github/workflows/run-jobs.yml` (`*/5`, GH-Actions floor) — same Hobby fallback as weekly-digest/youtube; `docs/cron-cadence.md` + publishing-setup + product tech-doc updated for the Pro `*/2` Vercel-cron path. Webhook comment processing deliberately **stays on `after()`** (latency-sensitive, already idempotent; poll-comments is its retry net) per H1. Env documented (`JOBS_*`, `PUBLISH_RECONCILE_BATCH`, `TRANSCRIBE_JOB_DEADLINE_MS`; dropped `PUBLISH_DUE_BATCH`/`AUTO_TRANSCRIBE_BUDGET_MS`). New vitest `test/jobs/queue.test.ts` (5 backoff cases) + `server-only` vitest alias → **59 tests / 7 files** green; typecheck + `next build` clean. Decisions: DB-cron over external queue (least resistance on this stack, per H1); attempts-on-claim so crashes count toward max_attempts; reconcile step covers legacy scheduled posts with no job; digest/transcribe stay no-op in prod until RESEND/GROQ keys are set (unchanged posture). Left: set repo `CRON_SECRET`+`APP_BASE_URL` for the new workflow (same as the other two GH crons). Next todo: V6 (Publishing GA) then V7.
- **2026-07-03 — V3 (W6), weekly niche digest email.** Migration `20260704004823_digest_opt_out.sql` (applied + verified via MCP; schema.sql + grants): `profiles.digest_opt_out boolean default false` with select+update grants to authenticated (mirrors brand_voice). Extracted `rankRising`/`risingSinceIso` from `feed/page.tsx` into shared `lib/reels/ranking.ts` (generic over `{viral_score, posted_at}`), feed now imports it (no behavior change). `lib/email/weekly-digest.ts` `buildWeeklyDigest()` → subject/html/text: top rising reels (Write-a-script + view links), saved-hook nudges (Use-in-script links), and a WLC prompt tailored to research-vs-scripts counts. `lib/email/digest-token.ts`: HMAC(user_id) keyed on CRON_SECRET for a login-free one-click unsubscribe URL. Cron `app/api/cron/weekly-digest/route.ts` (cronAuthorized; no-ops when email unconfigured): per opted-in user, builds id→email map via `auth.admin.listUsers`, gathers rising reels (rankRising top 5) + 3 saved hooks + 7-day research/script counts, sends via `sendEmail`, fires `digest_sent`; skips users with nothing to say. `app/api/account/digest-unsubscribe/route.ts` (GET, HMAC-verified → sets opt_out, confirmation page). Settings gains a **Weekly digest toggle** (`DigestToggle` + `setDigestOptOut` action). **Scheduling via `.github/workflows/weekly-digest.yml`** (Mon 08:00 UTC → curls the endpoint with CRON_SECRET) — same GH-Actions fallback as poll-youtube-comments, since Hobby's 2 daily cron slots are taken. Decisions: **GH Actions over a Vercel cron** (Hobby limit, works today without Pro); reused CRON_SECRET as the unsubscribe HMAC key (never exposed); digest no-ops until RESEND is wired (same posture as L9); verified the rising-reels join + digest_opt_out grants against live data. Left: set repo `CRON_SECRET` secret + `APP_BASE_URL` var for the workflow (same as the YouTube one), plus RESEND keys, to go live. Typecheck + `next build` + 54 tests clean.
- **2026-07-03 — V5 (W3), relative "Outperforming" score + why-tooltip.** Migration `20260703171233_outperforming_feed.sql` (applied + verified live via MCP; schema.sql updated): `outperforming_feed(p_user_id, p_account, p_group_ids, p_status, p_q, p_limit, p_offset)` SQL RPC, `security invoker` (RLS scopes reads) + `revoke anon`/`grant authenticated`. Ranks a user's feed by **relative_score = viral_score / greatest(followers_count, 1000)** (audience-normalized so small-niche accounts surface) and returns each reel's **outperform_ratio = viral_score / account_median** (median via `percentile_cont(0.5)`, cast to numeric — the double-precision return broke `round()` on first apply) plus a `count(*) over()` total for pagination. Replicates the feed's account/group/status/search filters in SQL (a cross-table ratio can't be a PostgREST `ORDER BY`). Wired into `feed/page.tsx`: **default sort is now "outperforming"**, its branch calls the RPC and maps the flat rows into `FeedReel` (nested `inspiration_accounts` + `outperform_ratio`); all other sorts keep the existing table query. Added "Outperforming" as the first `FeedControls` sort option and an **"Outperforming ×N" badge** (top-left thumbnail overlay, success color, `TrendingUp`) on `ReelCard` with a why-tooltip ("beat @acct's typical reel by ×N vs the account's median"). Verified live against a real 2,785-reel account: duncanrogoff (25k followers) ranked #1 at relative 5.83 / ×761 vs its median, above 100k-follower accounts — exactly the intended visibility flip. Decisions: **RPC over JS ranking** (correct pagination + true medians vs a capped candidate set); made it the default sort per W3 (the wedge value); floor followers at 1000 so tiny accounts don't explode. Deferred: W4's auto-suggest-save above an outperform threshold is now unblocked (separate follow-up, not in this change). Typecheck + `next build` + 54 tests clean.
- **2026-07-03 — V2 (W5), auto-transcribe top reels post-sync.** No migration (pure code). `lib/media/auto-transcribe.ts` → `autoTranscribeTopReels(admin, userId)`: selects the user's top-`viral_score` untranscribed reels (`transcript_status` null/none, not discarded; skips `failed`/`pending` so bad reels aren't re-hit and in-flight manual runs aren't duped), then transcribes up to `AUTO_TRANSCRIBE_TOP_N` (default 3) within a wall-clock budget (`AUTO_TRANSCRIBE_BUDGET_MS`, default 220s; stops if <30s left, per-reel deadline ≤150s). Reuses the exact manual-transcribe guards — hourly `consumeUserAction("transcript")` then monthly `consumeMonthlyQuota(tier,"transcripts_mo")` — and **stops the whole batch** when either denies (tier cap hit / rate-limited), so auto never exceeds the plan. Marks `pending` before each run (claim), `ready` on success (fires `transcript_ready {via:"auto"}`), `failed` on unavailable/timeout. Wired into `app/api/ig/sync/route.ts` via `after()` (post-response, detached) — only when the sync actually landed reels (`inserted||updated > 0`), so the 429/no-op paths skip it. Decisions: **service-role client** in the `after()` context (no cookie dependency post-response; quota still attributed via `p_user_id`), mirroring the webhook `after()` pattern; **gated on a configured Whisper provider** (`GROQ_API_KEY`/`HF_API_TOKEN`) so we never spuriously mark reels failed on an unconfigured server; default-ON with `AUTO_TRANSCRIBE_AFTER_SYNC=false` kill switch. This is the inline-`after()` interim per W5 — moves to the durable job queue at H1/V4. Env documented. Typecheck + `next build` + 54 tests clean.
- **2026-07-03 — V1 (W4), persistent hook library. First V1.1 item.** Migration `20260703165014_saved_hooks.sql` (applied via MCP, filename version-aligned; schema.sql updated): `saved_hooks` (user_id→profiles cascade, reel_id→tracked_reels **SET NULL** so a hook outlives its reel, `text`, `tags text[]`, `source` transcript|manual, `unique(user_id,text)`; RLS owner-only `for all` per tracked_reels; `created_at` btree + `tags` GIN index). Server actions `app/dashboard/hooks/actions.ts` (user-scoped client, RLS-enforced): `saveHook` (**upsert on (user_id,text)** so re-saving refreshes tags instead of erroring; tag normalize lower/hyphen/cap-12), `deleteHook`, `setHookTags`. Reworked `/dashboard/hooks` into two sections: **Saved hooks** (`SavedHooksLibrary` — text search + tag filter chips (OR semantics) + per-card inline tag add/remove with optimistic revert + delete + copy + "Use in script" + open-reel) and **From your transcripts** (existing `extractHook` suggestions, now each row has a **Save to library** button; already-saved marked via a `savedTexts` set). "Save hook" button added to `TranscriptPanel` (derives the opener via `extractHook`, saves with source=transcript). **"Use in script"** links to `/dashboard/scripts?hook=<text>`; `ScriptGenerator` gained `initialContext`, the scripts page reads the param and pre-frames it as `Open with this exact hook: "…"` into custom context. **Restored the Hooks nav entry** in Sidebar (Bookmark icon; was hidden). Decisions: hooks stored as explicit user saves (not derived) so the corpus is durable moat data; `unique(user_id,text)`+upsert makes save idempotent; framed the reused hook as an instruction rather than raw paste so the generator actually opens with it. Deferred from W4: auto-suggest-save for hooks above the outperform threshold (depends on V5's relative score, not built). Typecheck + `next build` + 54 tests all clean; verified table/FK/indexes/RLS live in prod via MCP.
- **2026-07-03 — L13 (H5), Vitest + money-logic tests. LAUNCH BLOCKERS COMPLETE.** Added Vitest 4 (`vitest.config.ts`: node env, `@/`→root alias mirroring tsconfig, `include: test/**`) + `test`/`test:watch` npm scripts. **54 tests across 6 files**, all green; `tsc --noEmit` and `next build` still clean (test/ lives outside app/, vitest ships its own types). Coverage, money-path first: `entitlements` (founder caps locked in, `withinLimit` at/over cap, free-automations paywall floor, unlimited never blocks, unknown-tier→free); `plans` (four tiers in order, `stripePriceIdForTier` env+trim, **`tierForStripePrice` reverse lookup** — the webhook's tier-assignment guard: known price→tier, unknown/empty→null so a legacy price can't mis-grant a tier); `subscription` (active vs past_due/trialing vs canceled/unpaid, invalid tier→free, **fail-open→null** on error/throw); `quota.consumeMonthlyQuota` (unlimited short-circuits the DB, allow/deny from RPC row, **fail-open allowed** on RPC error/throw); `tier.resolveUserTier` (active sub wins over env default, canceled ignored, unset/garbage `AI_DEFAULT_TIER`→free); `keyword-match` (contains token-boundary "link"≠"linkedin", punctuation/emoji/**Arabic** boundaries, regex metachars literal, exact, any). Helper `test/helpers/fake-supabase.ts` fakes the `from().select().eq().maybeSingle()` + `rpc()` chains and a throwing variant — no live DB. Added **`.github/workflows/ci.yml`** (npm ci → typecheck → `npm test` on push to master + all PRs) so the money-logic guard actually runs; postinstall yt-dlp fetch is non-fatal so CI install is safe. Decisions: mock at the Supabase-client boundary (fast, deterministic) rather than spin up local Supabase; deferred H5's JSON-repair unit tests and the RPC integration test (explicitly optional, and the RPC needs a live/local DB) — the entitlements slice the roadmap scoped for launch is done. **All 13 Launch blockers (L1–L13) are now `done`.** Remaining pre-launch work is non-code: Stripe UAE approval + keys, Meta app-review confirmation, and wiring `RESEND`/support mailboxes (see verification plan §8).
- **2026-07-03 — L12 (B8 + H6), legal minimum + PDPL delete/export.** One session, no migration (pure code + page copy). **`/terms`** (`app/terms/page.tsx`, reuses `LegalLayout`): service description, plans/billing/renewal via Stripe, a concrete **7-day full-refund** window (founder-set, defensible; cancel-anytime otherwise), acceptable use, content ownership + AI-output disclaimer, third-party-platform terms, termination, liability cap (3× trailing monthly), **UAE governing law**. Added Terms to the legal footer + login footer. **`/privacy`** rewritten: new **Sub-processors** section (Supabase, Vercel, Cloudflare R2, Stripe, Anthropic+NVIDIA, Groq+HF, Meta+Google, **Resend** from L9), retention/deletion pointing at self-serve Settings, export/delete rights, **breach-notification** commitment, and a **UAE PDPL** data-protection contact; "no sale / no sharing" stated. **`GET /api/account/export`** — admin-client JSON bundle across 10 user tables + profile/social_connections read with **explicit safe column lists (no tokens)**; gated by a new `account_export` rate-limit action (5/hr). **`POST /api/account/delete`** — requires typed `{confirm:"DELETE"}`; order: Meta `DELETE /me/permissions` (best-effort) → delete uploaded R2 videos+thumbnails (reads publish_posts first) → **explicit delete of app_events + ai_usage** (L5 tables have NO FK to profiles, so nothing cascades them) → **explicit delete of profiles** (cascades all profile-child app tables) → `auth.admin.deleteUser` (cascades subscriptions/user_monthly_usage). Settings gains a **"Data & privacy" card** (`components/settings/DangerZone.tsx`): export download link + a radix AlertDialog delete flow (type DELETE to arm) → redirect to /login. Decisions: **verified live FKs via MCP** — `profiles→auth.users` is NO ACTION (not cascade), so deleting the auth user before the profiles row would FK-fail; the route deletes profiles explicitly first (the original H6 audit's "profiles cascade wipes all rows" was pre-L5 and missed both this and the FK-less event tables). Left: `RESEND`/support-email addresses referenced in copy (`support@`/`privacy@reelspy.app`) need mailboxes; consider a signed re-auth step instead of typed-DELETE if higher assurance is wanted post-launch. Typecheck + `next build` clean.
- **2026-07-03 — L9 (B4), publish-failure notifications + honest `partial` status.** One session. **Honest status:** `dispatchPost` used to mark a post `done` whenever *any* target succeeded — hiding partial failures. Migration `20260703160610_publish_partial_status.sql` widens the `publish_posts` status CHECK to include `partial` (applied via MCP; repo filename renamed to the MCP-recorded version `20260703160610` so `db push` stays a clean no-op; schema.sql updated). The dispatcher now recomputes post status over **all** of the post's jobs (not just the ones that ran this pass) → `done` / `partial` / `failed`; this matters because `retryJob` re-dispatches a single job, so a naive "this run" count would wrongly flip a still-broken post to `done`. **Email:** `lib/email/send.ts` — Resend HTTP API wrapper (no SDK, one fetch), server-only, **fail-open** (no keys / send error ⇒ logs + returns false, never throws), gated on `RESEND_API_KEY`+`EMAIL_FROM`. `lib/email/publish-failure.ts` composes a per-platform failure digest (HTML + text, violet CTA, deep link to `/dashboard/publishing`) and fires once per publish with ≥1 failed target; recipient email fetched via `admin.auth.admin.getUserById` (profiles doesn't store email). Wrapped in try/catch in the dispatcher so notification failure can't affect the publish result. **UI:** added a `partial` badge style (warning) + a page-level "N posts didn't fully publish" banner above the history when any post is partial/failed with failed jobs; per-job `RetryButton` + error text already existed (from earlier work), so retry needed no change. **Instrumentation:** wired the L5-deferred `publish_job_finished` event (`{platform, status: success|failed, post_id}`) on every job outcome → feeds the `publish_success_weekly` view. Decisions: Resend over Supabase SMTP (simplest HTTP, no SMTP creds); email uses overall (all-jobs) success count for "partial vs all-failed" wording so retry emails read correctly; retry-that-fails-again re-emails (acceptable, user-initiated). Env docs added to `.env.example`. Left: founder adds `RESEND_API_KEY`/`EMAIL_FROM` (emails no-op until then); Resend should be added to the processor disclosure in L12/B8. Typecheck + `next build` clean.
- **2026-07-03 — L11 (B9) + L10 (B7), palette rebrand + empty/error/loading states.** Two commits. **L11:** `globals.css` swapped the neon-yellow brand to violet (`--primary`/`--accent`/`--ring` = `#6D28D9` light / `#7C3AED` dark, `--primary-foreground` → white, `--brand` = violet), shifted dark surfaces to the ink/slate family (`#0F172A` bg, `#1E293B` card, slate borders), and added theme-adaptive semantic status tokens `--success`/`--warning`/`--danger`/`--info` (registered in `@theme inline`; darker shades on light, brighter on dark). A PowerShell sweep stripped the shade from ~90 hardcoded `emerald-/amber-/rose-/sky-/blue-` classes across 41 files → `success`/`warning`/`danger`/`info`. The old on-yellow idiom `text-black` (12 spots on `bg-primary`) → `text-primary-foreground`; destructive confirm button → `bg-destructive`. Decisions: kept a distinct `--danger` (status text) vs `--destructive` (solid action buttons); **excluded 3 decorative files** (`PublishPreview` IG gradient ring + Facebook/link brand blues, `FavoriteButton` heart) so brand-mimicking color survives; folded `blue` status (ready/scheduled) into `--info` for coherence. Fixed the yellow sheen rgba → violet. **L10:** added 6 `loading.tsx` skeletons (scripts/accounts/generate/publishing/automations/my-account) mirroring each page's layout (feed + dashboard already had them). Feed first-run empty state now branches on a new `hasAccounts` count → routes zero-account users to `/dashboard/onboarding` ("Set up ReelSpy" + "Add accounts manually" fallback) instead of telling them to sync nothing; non-first-run copy reworded. `ScriptGenerator` gained a `transcriptStatus` prop → **upfront** degraded-grounding hint (failed = warning styling; pending/none = muted) so the transcript panel's failure is reflected before the user generates. Left as-is (already adequate): `ScriptsList` empty/filter states, `SyncButton` failure path (toast + 429 cooldown + re-clickable button = the retry). Both commits: typecheck + `next build` clean.
- **2026-07-03 — Supabase go-live + migration reconciliation (infra).** User connected the Supabase↔GitHub integration and gave MCP access. Found the DB was BEHIND the code by four migrations — `brand_voice` (L1), `app_events`+`ai_usage` (L5), the 5 instrumentation views (L5), and billing (L6) had NEVER been applied (L5 `track()` was silently no-oping). Applied all via MCP `apply_migration`. Security-advisor pass caught the L5 views as SECURITY DEFINER (per-user data leak via PostgREST) → fixed with `security_invoker=on` + `revoke` from anon/authenticated; also revoked `consume_user_action_monthly` execute from anon. All `security_definer_view` ERRORs cleared. Then **rebaselined migration history**: repo filenames were short/letter-suffixed (`20260703d_billing.sql`) and `schema_migrations` had drifted, so the integration's `db push` would have re-run applied migrations and failed. Renamed all 27 files to canonical `YYYYMMDDHHMMSS_name.sql` (order preserved) and rewrote `schema_migrations` to exactly match (backup: `schema_migrations_bak_20260703`) → `db push` is now a clean no-op. Added `supabase/migrations/README.md` (naming rule + the `schema.sql`-is-the-base caveat: migrations aren't self-contained, a fresh DB/branch needs `schema.sql` first). Mirrored all hardening + the 5 views into `schema.sql`.
- **2026-07-03 — L7 (B3), onboarding wizard to <10-min activation.** Shipped in 4 commits. `profiles.onboarded_at` (migration `20260703000004`, applied via MCP + history aligned; schema.sql + grants). `lib/onboarding/state.ts`: `getOnboardingState` infers step completion (source / brand voice / accounts / synced / first script) from real data — only `onboarded_at` is persisted — and returns `currentStep`/`completedCount`/`activated`. `/dashboard/onboarding`: URL-driven (`?step=`) 4-step wizard so data mutations (which refresh in place) don't yank the user forward. Step 1 = Connect Instagram OR `seedStarterPack` (zero-Meta-quota seed from `ig_account_snapshots`, top-followed, trimmed to the plan account cap); step 2 = `BrandVoiceForm` → `saveBrandVoice` (per-field caps, feeds B2); step 3 = inline bulk add (reuses `bulkAddInspirationAccounts`) + starter pack; step 4 = branches on data (write from top-velocity reel / `SyncButton` / connect) then `finishOnboarding` sets `onboarded_at`. `components/onboarding/OnboardingControls.tsx` = client controls importing server actions directly. Dashboard home: first-run redirect (nothing done + not onboarded → wizard) + `SetupChecklist` card (shown until activated/dismissed). `onboarding_step` events fire (brand_voice/starter_pack/complete); rest of the funnel already instrumented (L5). Decisions: step inferred from data (not stored) so out-of-band actions stay consistent; `creator` brand-voice field derived elsewhere, not asked; `Instagram` lucide icon unavailable in this version → used `Camera` (the codebase's IG glyph). Left: starter-pack (non-connected) users can't sync reels (BD needs a connection) so their step-4 nudges toward connecting — acceptable, connection is the primary ICP path. Typecheck + full `next build` clean.
- **2026-07-03 — L6 (B1), Stripe UAE billing + entitlements.** Shipped in 4 commits. Migration `20260703d_billing.sql`: `subscriptions` (user_id PK, stripe_customer/subscription_id, tier, status, current_period_end, cancel_at_period_end — RLS: owner SELECT only, service-role webhook is the sole writer; SELECT grant only, no client write grants) + `user_monthly_usage` + `consume_user_action_monthly(user, action, limit)` RPC (calendar-month window, negative limit = unlimited, returns allowed/used/remaining/period_end). `lib/billing/`: `entitlements.ts` (per-tier caps: accounts/scripts_mo/transcripts_mo/automations/publish_targets, `UNLIMITED=-1`, `withinLimit`/`limitFor`/`formatLimit`), `plans.ts` (tier→Stripe Price via env + display meta + `tierForStripePrice` reverse lookup), `subscription.ts` (fail-open `getSubscription`/`activeTierFromSubscription`), `quota.ts` (`consumeMonthlyQuota` wiring entitlements→RPC, fail-open), `stripe.ts` (lazy client, null when unkeyed, `siteOrigin`). `resolveUserTier` now reads the subscription (active sub wins, else `AI_DEFAULT_TIER`). Enforcement at 4 chokepoints: accounts (add: new-account-only cap; bulk: trims to remaining slots, reports `limited`), scripts/mo + transcripts/mo (monthly quota → 402 `{upgrade:true}` in generate-script + per-reel transcript + reel-from-link), automations (per-tier cap in createAutomation, Free=0 → free→paid gate). Routes: `POST /api/billing/checkout` (subscription Checkout, get-or-create customer, user_id on session+sub metadata), `POST /api/billing/portal`, `POST /api/stripe/webhook` (signature-verified sole writer — syncs from checkout.session.completed + customer.subscription.*, maps customer→user, drops to free on lapse/cancel, 500s to force Stripe retry). `/dashboard/billing` page (current plan + usage meters + 4-tier pricing grid, upgrade/switch buttons, success/cancel banners, preview mode when unconfigured) + `BillingActions` client component + sidebar plan badge (`SidebarUser.tier`). Added `stripe@^17`; env + `docs/billing-setup.md`. Decisions: fail-open everywhere (missing table/keys ⇒ allow + env-default tier, so the app builds/runs before Stripe UAE approval); monthly slots consumed on valid genuine runs only; apiVersion pinned to `2025-02-24.acacia` (stripe@17 LatestApiVersion); `constructEventAsync` (Web Crypto). Left: user confirms Stripe UAE billing + adds keys (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_*`); apply the migration in the Supabase dashboard (no DDL access from this machine); wire `subscription_changed` instrumentation event (deferred from L5). Typecheck clean.
- **2026-07-03 — L5 (§5), instrumentation + WLC views.** Shipped: `app_events` + `ai_usage` tables (migration `20260703b`, service-role only, RLS no policies; also in schema.sql). `lib/analytics/track.ts` — `track(userId, event, props)` + `trackAiUsage(userId, row)`, both self-create the admin client and swallow ALL errors (incl. missing service-role key) so instrumentation can never break a request. `chat()` now surfaces token usage: `ChatResult.usage` (`{model, inputTokens, outputTokens}`) plumbed from both providers (NVIDIA `usage.prompt/completion_tokens`, Anthropic `response.usage`); `generateScript`/`generateGrowthNotes` return `provider`+`usage`. Migration `20260703c` defines 5 views: `wlc_weekly` (North Star), `activation_funnel` (+<10-min SLA flag), `retention_cohorts`, `publish_success_weekly`, `ai_cost_per_user` (token×price map). Events WIRED: `signed_up` (auth/callback, first-session heuristic on user.created_at), `ig_connected` (ig/callback), `account_added` (accounts/actions, single+bulk), `feed_synced` (ig/sync), `transcript_ready` (transcript route + reel-from-link), `script_generated`+`ai_usage:script` (generate-script), `ai_usage:growth_notes` (growth-notes). This makes WLC + activation funnel + per-user AI cost fully measurable. DEFERRED events (secondary, and some gated on unbuilt features): `script_scheduled` (scripts/actions), `post_created`/`publish_job_finished` (publishing/dispatcher — feeds publish_success view + WLC-alt output), `automation_fired` (processors), `onboarding_step` (L7/B3), `hook_saved` (V1/W4), `subscription_changed` (L6/B1). Their views already exist and return empty until wired. Typecheck clean.
- **2026-07-03 — L3 (W2), Claude for paid tiers + tool-use JSON.** Consulted the claude-api skill for model IDs/tool-use. Shipped: `lib/ai/provider.ts` `chat()` now routes by tier — paid (Creator/Pro/Studio) → Claude when `ANTHROPIC_API_KEY` set (Haiku 4.5 for Creator, Sonnet 4.6 for Pro/Studio, via `anthropicModelForTier`), free → NVIDIA Llama; each falls back to the other provider if its key is missing. `callAnthropic` gained a forced tool-use path: when a `JsonTool` is passed it forces `tool_choice` on that tool with `strict: true` and returns `tool_use.input` as clean JSON — no quote/control-char/salvage repair needed on the Claude path (that stack stays for NVIDIA). `lib/ai/claude.ts` defines `emit_script`/`emit_growth_notes` tools and threads `tier`; both routes resolve tier and pass it. New `lib/ai/tier.ts`: `resolveUserTier(supabase, userId)` — env-driven (`AI_DEFAULT_TIER`, default free) until L6, with the signature already taking `userId` so L6 swaps in the subscriptions lookup with zero caller changes. Decisions: Haiku alias (not dated ID) per skill's alias preference; no `thinking`/`effort` params (Haiku doesn't support effort, and plain calls avoid 400s); tool-use added to growth notes too (same cheap mechanism). Deferred (still open in W2): growth-notes weekday/hour-bucket metrics enrichment, and per-platform structure blocks / tone definitions in the script prompt — separable prompt tuning, not blocking. Env docs added. Typecheck clean.
- **2026-07-03 — L2 (W1), transcript-grounded scripts.** Shipped: `generate-script` route now selects `transcript, transcript_status, viral_score, view_count, posted_at`; when the transcript is `ready` it's passed to `generateScript` as the primary source and the caption becomes secondary. `lib/ai/claude.ts`: added transcript/perf grounding fields to `GenerateScriptInput`, a `<reel_transcript>` block (capped 8k chars) ordered before `<reel_caption>`, a "Source reel performance: score/views/posted N days ago" line, and updated the system prompt to ground on the transcript + extend the injection guard to `<reel_transcript>`. Route returns a `grounded` flag. `ScriptGenerator`: shows "Grounded on transcript ✓" (emerald) or "Caption only" chip, plus a one-tap "Transcribe first, then regenerate" that POSTs the existing transcript pipeline then re-runs generation. Decisions: only a `ready` transcript grounds (pending/failed → caption-only, not a hard error); chips gated on `reelId` so the paste-caption path is unaffected. Colors use raw emerald/amber/border classes to match the file — the L11 rebrand sweep will tokenize them. Typecheck clean.
- **2026-07-03 — L1 (B2), de-persona AI prompts.** Shipped: `brand_voice jsonb` on `profiles` (migration `20260703_profile_brand_voice.sql` + schema.sql, with select/update grants to `authenticated` — token-column posture untouched). `lib/ai/claude.ts`: replaced the two hardcoded `@majdst_codes` `SCRIPT_/GROWTH_SYSTEM_PROMPT` constants with `buildScriptSystemPrompt`/`buildGrowthSystemPrompt(bv)` builders + `BrandVoice` type; fields are length-capped before interpolation (self-injection/token guard); neutral creator persona when unset. Both AI routes read `profiles.brand_voice` and pass it through. Decisions: jsonb over columns (prompt-context only, never queried); de-dev-ified the generic fallback copy ("the audience", "a real example") so it reads for any niche. Left: the brand-voice **collection form** is part of onboarding (L7/B3) — until then every user gets the neutral persona, which is already correct/sellable. Typecheck clean.
- **2026-07-03 — L4 + L8 (B6, B5).** Shipped: (1) `reel-from-link` now calls `consumeUserAction(..., "transcript")` before the yt-dlp+Whisper pipeline — reuses the existing `transcript` bucket since it's the same pipeline. (2) New `upload_presign` action (default 60/hr, `RL_UPLOAD_PRESIGN_PER_HOUR`) enforced in `publishing/upload`. (3) `diag?transcribe=1` gated behind `DIAG_ALLOWED_USER_IDS` allowlist, **fail-closed** (unset ⇒ nobody); cheap metadata-only diag path stays open. (4) `vercel.json`: `publish-due` `0 0 * * *` → `*/5 * * * *`, added `poll-comments */10 * * * *`. No DB migration (RPC takes arbitrary `p_action`). Decisions: reel-from-link shares the transcript quota rather than a new bucket; diag fails closed rather than throttling. Left: L8 cadence assumes Vercel Pro is live before launch (founder decision #3); GH Actions fallback per B5 if Pro is rejected.
