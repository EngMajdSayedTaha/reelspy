// Stub for Next.js's `server-only` package, which is a bundler-resolved marker
// with no runtime and no node-resolvable module. Aliased in vitest.config.ts so
// server-only lib modules (e.g. lib/jobs/queue.ts) can be unit-tested.
export {};
